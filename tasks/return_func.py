def sum(x, y):
    summary = x + y
    return summary


def super_sum(s, d, z, p, o, u):
    a = sum(s, d)
    b = sum(z, p)
    c = sum(o, u)

    summary = a + b + c

    return summary


# wynik = super_sum(1, 2, 3, 4, 5, 6)
# print(wynik)